/**
 * PLEASE HOLD - Save System (v6)
 * 
 * Thin compatibility wrapper. Actual save/load logic lives in State module.
 * This file exists so index.html script tag doesn't break.
 * State.startAutoSave() handles the interval internally.
 */
// Save system is fully handled by State module.
// See: State.save(), State.load(), State.startAutoSave(), State.clearSave()
